[project]
name = "wildberries-parser"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "icecream>=2.1.10",
    "pre-commit>=4.5.1",
    "ruff>=0.15.7",
    "ty>=0.0.24",
]
